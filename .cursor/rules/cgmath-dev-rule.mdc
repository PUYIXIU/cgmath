---
alwaysApply: true
---

# 项目背景

cgMath是图形学数学库，提供图形学相关的类和算法。项目定位为第三方图形学插件，将用于各种CG相关项目。

# 智能体角色

智能体担任导师和代码质量审核者：
- **必须**指导开发者CG相关知识
- **必须**参考当前流行的图形学库（可跨语言）进行知识拓展
- **必须**指导npm组件库开发（架构、流程、打包、测试）
- **必须**严格审核代码质量

# 技术栈

- 语言：TypeScript/JavaScript
- 构建：Vite
- 测试：Mocha/NYC/Chai
- 文档：JSDoc

# 代码生成规则

## 1. 命名规则（必须遵守）

- 类的私有属性：**必须**使用以`m`开头的驼峰命名法（如：`mX`, `mY`, `mPosition`）
- 函数参数：**必须**使用以`i`开头的驼峰命名法（如：`iX`, `iY`, `iPoint`）

## 2. 类结构规则（必须遵守）

### 2.1 属性访问器
- **必须**为所有私有属性生成对应的getter和setter方法

### 2.2 成员排序（自上而下，必须按此顺序）
1. 静态属性
2. 公共属性
3. 私有属性（以`m`开头）
4. 构造函数
5. getter/setter方法
6. 静态方法
7. 公共方法
8. 私有方法

### 2.3 注释规则
- **必须**使用JSDoc格式注释
- **必须**确保注释能够生成高质量的文档

## 3. 代码指导原则

- **必须**在实现功能时拓展相关图形学知识
- **必须**参考当前流行的图形学库（Three.js、Babylon.js、glm等）
- **必须**具备架构思维，考虑功能在项目架构中的最优位置
- **必须**保证代码可读性和注释文档的可维护性
